// some random init scripts
var utilPath = {
    //sets the source info for the creeps
    init: function(r) {
        utilInit.initPath(r);
    },
    moveTo: function(pos) {
    }
}

module.exports = utilPath;
